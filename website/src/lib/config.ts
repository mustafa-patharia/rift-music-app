// Single source of truth for external links.
export const REPO = "https://github.com/MustafaPatharia/rift-music-app";
export const RELEASE = `${REPO}/releases/latest`;
// Fallback release meta (release.js live-updates these when possible).
export const REL_VERSION = "v1.0.1";
export const REL_SIZE = "";
